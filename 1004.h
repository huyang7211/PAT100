#include <iostream>
#include <map>
#include <string>
#include <vector>
using namespace std;
class Student
{
private:
	string name;
	string id;
public:
	string getName();
	string getId();
	void setName(string name);
	void setId(string id);
};

int main1004();